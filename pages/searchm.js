import { useEffect } from "react";
import Layout from "@/components/Layout";

const Mapa= () => {
  return (
    <Layout
      pagina="Tienda Virtual"
    >
      <h1>Desde buscar mapa</h1>
  </Layout>
  )
}

export default Mapa